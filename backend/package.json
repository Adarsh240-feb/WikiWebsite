{
  "name": "wikiwebsite-backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
    ,"vercel-build": "cd ../frontend && npm ci --prefer-offline --no-audit --progress=false && npm run build",
    "postvercel-build": "node ./scripts/copy-dist.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "mongodb": "^6.20.0",
    "wikiwebsite": "file:.."
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
